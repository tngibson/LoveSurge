using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PointChecker : MonoBehaviour
{
    //[SerilializeField] private ConvoTopic convoTopic;
    //[SerilializeField] private PlayTutorialDialog dialog;

    // Start is called before the first frame update
    void Start()
    {
        //GetComponent<ConvoTopic>()
    }

    // Update is called once per frame
    void Update()
    {
        //Check if convo topic power tier < 0
        //play dialog
    }
}
